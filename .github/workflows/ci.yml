name: Backend CI

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

jobs:
  build-and-test:
    name: Build, Lint & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.24" # ATUALIZADO: Alinhado com seu go.mod
          cache: true

      - name: Install Dependencies
        run: go mod download

      - name: Run Linter (GolangCI-Lint)
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          # Importante: garante que o linter use a versão do Go instalada acima
          args: --timeout=5m

      - name: Run Tests
        run: go test -v ./...

      - name: Build Check
        run: go build -v ./cmd/api
